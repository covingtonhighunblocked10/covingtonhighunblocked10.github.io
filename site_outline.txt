The site opens to index.html, where on clicking the logo, the user will be transported to the home.html file.
All sites should use the nav and title used in the home.html file. If not, the color scheme used for the site should be kept.
Mark your work in a comment if you made a significant change so that we can know who made it.
All pushes should be prefaced with "<your name>: " and then your change.
Example push:
git add .
git commit -m "Jace: making site_outline.html file"
git push

Example Pull:
git pull